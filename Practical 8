Practical 8:
Q1.Write Function sumOfDigits that take n as input and return sum of its digits 
       Eg n = 4123  output = 10
code here:-
#include<stdio.h>
int sumOfDigits(int n){
    int sum = 0;
    if(n<0){
        n=-n;   
    }
    while(n>0){
        sum +=n%10;
        n/=10;
    }
    return sum;
}
int main() {
    int n;
    printf("Enter a Number: ");
    scanf("%d",&n);
    printf("%d", sumOfDigits(n)); 
    return 0;
}
Q2.Swap 2 numbers using pass by reference.
code here:-
#include <stdio.h>
void swap(int*a,int*b){
    int temp;
    temp =*a;
    *a=*b;
    *b=temp;
}
int main() {
    int x, y;
    printf("Enter two numbers:\n");
    scanf("%d %d",&x,&y);
    printf("Before swapping:x=%d,y=%d\n",x,y);
    swap(&x, &y); 
    printf("After swapping: x=%d, y=%d\n",x,y);
    return 0;
}

Q3.Write a function to prints a right angle triangle of stars of height n.
code here:-
#include <stdio.h>
void printTriangle(int n){
    for (int i=1;i<=n;i++){
        for (int j=1;j<=i;j++){
            printf("*");
        }
        printf("\n");
    }    
}
int main(){
    int n;
    printf("Enter a Number: ");
    scanf("%d",&n);
    printTriangle(n);
    return 0;
}
